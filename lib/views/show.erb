<div class="card mb-4 shadow-lg">
  <div class="card-header bg-primary text-white">
    Potin n°<%= id %>
  </div>
  <div class="card-body">
    <blockquote class="blockquote mb-0">
      <p class="mb-4 display-6">"<%= gossip.content %>"</p>
      <footer class="blockquote-footer">Écrit par <cite title="Source Title"><%= gossip.author %></cite></footer>
    </blockquote>
    
    <div class="mt-4">
      <a href="/gossips/<%= id %>/edit/" class="btn btn-warning btn-sm">Modifier</a>
      <a href="/" class="btn btn-secondary btn-sm">Retour</a>
    </div>
  </div>
</div>

<div class="card bg-light p-3">
  <h5>💬 Commentaires</h5>
  <hr>
  
  <% comments.each do |comment| %>
    <div class="alert alert-secondary">
      <%= comment.content %>
    </div>
  <% end %>

  <form action='/gossips/<%= id %>/comments/' method='POST' class="d-flex mt-3">
    <input type='text' name='comment_content' class="form-control me-2" placeholder='Réagir à ce potin...' required />
    <button type='submit' class="btn btn-dark">Envoyer</button>
  </form>
</div>